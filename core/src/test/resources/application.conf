postgres-starved {

  user = ${PG_USER}
  user = "pguser"
  password = ${PG_PASSWORD}
  password = "pguser"
  host = ${PG_HOST}
  host = "0.0.0.0"
  port = ${PG_PORT}
  port = 5432
  database = ${PG_DATABASE}
  database = "pgdb1"
  maximumPoolSize = 1
  minimumIdle = 1

}